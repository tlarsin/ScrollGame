#:kivy 1.0.9

<SpriteCharacter>:
    size: 15,30
    canvas:
        Rectangle:
            source: 'sprite.bmp'
            pos: self.pos
            size: self.pos

<RunGame>:
    sprite: character

    SpriteCharacter:
        id: character
        center_x: root.center_x
        #y: blocks.height
